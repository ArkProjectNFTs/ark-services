#!/bin/sh
set -e

## This will only remove masks created by d-s-h on package removal.
deb-systemd-helper unmask ark-metadata-marketplace-staging.service > /dev/null || true
systemctl daemon-reload

# was-enabled defaults to true, so new installations run enable.
if deb-systemd-helper --quiet was-enabled ark-metadata-marketplace-staging.service
then
	# Enables the unit on first installation, creates new
	# symlinks on upgrades if the unit file has changed.
	deb-systemd-helper enable ark-metadata-marketplace-staging.service > /dev/null || true
	systemctl enable ark-metadata-marketplace-staging.service
else
	# Update the statefile to add new symlinks (if any), which need to be
	# cleaned up on purge. Also remove old symlinks.
	deb-systemd-helper update-state ark-metadata-marketplace-staging.service > /dev/null || true
fi